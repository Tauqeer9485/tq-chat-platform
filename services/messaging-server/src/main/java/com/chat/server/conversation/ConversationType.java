package com.chat.server.conversation;

/**
 * Enum representing the type of a conversation
 * DIRECT: A one-on-one conversation between two users
 * GROUP: A conversation involving multiple users
 */
public enum ConversationType {
    DIRECT,
    GROUP
}